"""Reusable rendering building blocks, shared across renderers and families.

Distinct from `sheets/`: a sheet is one CSS template *for one document
family* (letterhead, colours, paper size all specific to it); a component
here has no family of its own and no opinion on what document it ends up in
-- see `table.py`.
"""
