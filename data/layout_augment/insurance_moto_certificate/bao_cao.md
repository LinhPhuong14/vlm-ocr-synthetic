# `insurance_moto_certificate`

Chứng từ dùng để dựng: `insurance_moto_certificate` — hạng **locked**.

Phôi gốc: `goc.jpg` (proof: `../proof/insurance_moto_certificate/goc.jpg`). Tất cả các bản dưới đây vẽ từ **cùng một hạt giống, cùng một chứng từ, tắt hết hiệu ứng làm cũ và không đóng dấu** — nên khác nhau chỗ nào thì đúng là bố cục khác nhau chỗ ấy.

## `v1_the_dai_ngang`

**Thẻ dải ngang: mỗi khối một dải nền, bề rộng so le**

Đo được: **54%** số ô chữ đã đổi chỗ so với phôi (13/25 ô ghép được, 1 ô chỉ có ở một bên; đã trừ độ dịch chung [-4.0, -4.0]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| nền khối | giấy trắng suốt | dải nền xám và trắng xen kẽ, khối đầu nền tối chữ đảo màu |
| bề rộng khối | mọi khối rộng bằng nhau | khối lẻ hụt 14% bên phải, khối chẵn hụt 14% bên trái |

Ảnh: `v1_the_dai_ngang.jpg` — proof: `proof/insurance_moto_certificate/v1_the_dai_ngang.jpg`

## Những bản bị loại

Vẽ ra rồi bỏ đi, vì bộ phản biện bắt lỗi nặng hoặc vì đo ra không đổi gì — nghĩa là CSS của thiết kế ấy không với tới được họ giấy này. Thiết kế kế tiếp trong danh sách được lấy thay.

| thiết kế | lý do |
|---|---|
| `the_hai_mang` | 3 lỗi nặng (tran_le) |
| `the_nen_chat` | không đổi gì (0.0%) |
| `the_chia_hai_cot` | không đổi gì (0.0%) |
| `the_lech_phai` | 1 lỗi nặng (tran_le) |
