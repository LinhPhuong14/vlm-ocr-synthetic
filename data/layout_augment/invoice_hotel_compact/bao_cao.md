# `invoice_hotel_compact`

Chứng từ dùng để dựng: `hotel_stay` — hạng **free**.

Phôi gốc: `goc.jpg` (proof: `../proof/invoice_hotel_compact/goc.jpg`). Tất cả các bản dưới đây vẽ từ **cùng một hạt giống, cùng một chứng từ, tắt hết hiệu ứng làm cũ và không đóng dấu** — nên khác nhau chỗ nào thì đúng là bố cục khác nhau chỗ ấy.

## `v1_cot_nhan_dien_trai`

**Dải nhận diện dọc bên trái, nội dung dồn phải**

Đo được: **92%** số ô chữ đã đổi chỗ so với phôi (71/78 ô ghép được, 5 ô chỉ có ở một bên; đã trừ độ dịch chung [72.3, 8.3]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| kiến trúc trang | một cột dọc, lề đều hai bên | hai vùng: dải nhận diện 21mm bên trái, khối chữ thụt vào 26mm, riêng bảng hàng chạy tràn hết bề ngang |
| bộ chữ | chữ có chân | chữ không chân toàn trang |
| bảng hàng | kẻ ô bốn cạnh | bỏ kẻ dọc, tiêu đề nền xanh chữ trắng |
| khối chữ ký | canh giữa hoặc hai cột đều | dồn về mép nội dung bên phải |

Khối được xếp lại: `swap` letterhead → doctitle

Ảnh: `v1_cot_nhan_dien_trai.jpg` — proof: `proof/invoice_hotel_compact/v1_cot_nhan_dien_trai.jpg`

## `v2_bang_thanh_the`

**Bảng hàng thành thẻ nổi trên nền có màu**

Đo được: **94%** số ô chữ đã đổi chỗ so với phôi (72/78 ô ghép được, 16 ô chỉ có ở một bên; đã trừ độ dịch chung [100.2, 74.2]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| nền trang | giấy trắng | nền xám xanh, từng khối là thẻ trắng nổi |
| khối nội dung | chảy liền nhau trên một tờ, cùng lề | mỗi khối một thẻ riêng bo góc có bóng, so le trái/phải 26% |
| bảng hàng | kẻ ô đầy đủ | bỏ hết kẻ, chỉ còn kẻ chân dòng nhạt |
| bộ chữ | chữ có chân | chữ không chân, dòng thoáng hơn |

Khối được xếp lại: `after` words → signatures

Ảnh: `v2_bang_thanh_the.jpg` — proof: `proof/invoice_hotel_compact/v2_bang_thanh_the.jpg`

## `v3_hai_cot_so_le`

**Khối lẻ dồn trái, khối chẵn dồn phải, bảng trải hết**

Đo được: **97%** số ô chữ đã đổi chỗ so với phôi (75/78 ô ghép được, 36 ô chỉ có ở một bên; đã trừ độ dịch chung [104.6, 323.6]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| kiến trúc trang | mọi khối trải hết bề ngang | khối lẻ chiếm 2/3 bên trái, khối chẵn 2/3 bên phải, so le nhau |
| bảng hàng | thụt theo khung | trải hết bề ngang, phá thế so le |
| thứ tự khối | tiêu đề trên letterhead | letterhead lên trước, số tiền bằng chữ xuống dưới chữ ký |

Khối được xếp lại: `swap` letterhead → doctitle; `after` words → signatures

Ảnh: `v3_hai_cot_so_le.jpg` — proof: `proof/invoice_hotel_compact/v3_hai_cot_so_le.jpg`

## `v4_bang_dan_dau`

**Bảng hàng lên đầu trang, khối nhận diện xuống dưới**

Đo được: **100%** số ô chữ đã đổi chỗ so với phôi (78/78 ô ghép được, 12 ô chỉ có ở một bên; đã trừ độ dịch chung [-6.7, 43.1]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| thứ tự đọc | nhận diện → bảng → tổng → ký | bảng → tổng → nhận diện → ký: đảo hẳn trục dọc của tờ giấy |
| canh khối | mọi khối cùng lề trái | từ khối thứ ba trở đi thụt vào 18%, tạo bậc thang |
| dải tiêu đề bảng | nền xám nhạt | nền đen chữ trắng |

Khối được xếp lại: `before` table → parties; `after` words → signatures; `before` footer → signatures

Ảnh: `v4_bang_dan_dau.jpg` — proof: `proof/invoice_hotel_compact/v4_bang_dan_dau.jpg`

## `v2_bac_thang_phai`

**Bậc thang: mỗi khối thụt sâu hơn khối trên nó**

Đo được: **85%** số ô chữ đã đổi chỗ so với phôi (66/78 ô ghép được, 3 ô chỉ có ở một bên; đã trừ độ dịch chung [12.8, 75.7]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| kiến trúc trang | mọi khối cùng lề trái | bậc thang: khối thứ hai thụt 6%, thứ ba 12%, từ thứ năm trở đi 24% |
| bảng hàng | theo lề chung | kéo về lề trái, phá thế bậc thang |
| thứ tự khối | ký rồi tới chân trang | chân trang lên trước ký |

Khối được xếp lại: `before` footer → signatures; `after` words → signatures

Ảnh: `v2_bac_thang_phai.jpg` — proof: `proof/invoice_hotel_compact/v2_bac_thang_phai.jpg`

## `v3_phieu_dong_dau_lon`

**Phiếu nén chặt, khối ký đóng khung đưa lên trước khối tổng**

Đo được: **100%** số ô chữ đã đổi chỗ so với phôi (78/78 ô ghép được, 15 ô chỉ có ở một bên; đã trừ độ dịch chung [33.8, 113.1]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| khối chữ ký | nằm cuối trang, không khung | đóng khung, đưa lên trước khối tổng |
| kiến trúc trang | mọi khối cùng lề | khối chẵn thụt trái 12%, khối lẻ hụt phải 12%, so le |
| mật độ | dòng chuẩn | nén chặt: dòng 1.2, ô bảng mỏng |
| khung trang | viền đơn | viền đôi |

Khối được xếp lại: `before` signatures → totals; `before` footer → signatures

Ảnh: `v3_phieu_dong_dau_lon.jpg` — proof: `proof/invoice_hotel_compact/v3_phieu_dong_dau_lon.jpg`

## Những bản bị loại

Vẽ ra rồi bỏ đi, vì bộ phản biện bắt lỗi nặng hoặc vì đo ra không đổi gì — nghĩa là CSS của thiết kế ấy không với tới được họ giấy này. Thiết kế kế tiếp trong danh sách được lấy thay.

| thiết kế | lý do |
|---|---|
| `dan_bao_chi` | 1 lỗi nặng (khong_muc) |
| `so_cai_khong_vien` | 1 lỗi nặng (khong_muc) |
| `bang_ron_toi` | 1 lỗi nặng (khong_muc) |
| `cot_giua_hep` | 5 lỗi nặng (tran_le) |
