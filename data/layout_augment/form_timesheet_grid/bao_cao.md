# `form_timesheet_grid`

Chứng từ dùng để dựng: `form_roster` — hạng **livery**.

Phôi gốc: `goc.jpg` (proof: `../proof/form_timesheet_grid/goc.jpg`). Tất cả các bản dưới đây vẽ từ **cùng một hạt giống, cùng một chứng từ, tắt hết hiệu ứng làm cũ và không đóng dấu** — nên khác nhau chỗ nào thì đúng là bố cục khác nhau chỗ ấy.

## `v1_cot_nhan_dien_trai`

**Dải nhận diện dọc bên trái, nội dung dồn phải**

Đo được: **100%** số ô chữ đã đổi chỗ so với phôi (82/82 ô ghép được, 0 ô chỉ có ở một bên; đã trừ độ dịch chung [-40.0, -63.1]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| kiến trúc trang | một cột dọc, lề đều hai bên | hai vùng: dải nhận diện 21mm bên trái, khối chữ thụt vào 26mm, riêng bảng hàng chạy tràn hết bề ngang |
| bộ chữ | chữ có chân | chữ không chân toàn trang |
| bảng hàng | kẻ ô bốn cạnh | bỏ kẻ dọc, tiêu đề nền xanh chữ trắng |
| khối chữ ký | canh giữa hoặc hai cột đều | dồn về mép nội dung bên phải |

Khối được xếp lại: `swap` letterhead → doctitle

Ảnh: `v1_cot_nhan_dien_trai.jpg` — proof: `proof/form_timesheet_grid/v1_cot_nhan_dien_trai.jpg`

## `v2_bang_thanh_the`

**Bảng hàng thành thẻ nổi trên nền có màu**

Đo được: **100%** số ô chữ đã đổi chỗ so với phôi (82/82 ô ghép được, 0 ô chỉ có ở một bên; đã trừ độ dịch chung [222.0, -55.0]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| nền trang | giấy trắng | nền xám xanh, từng khối là thẻ trắng nổi |
| khối nội dung | chảy liền nhau trên một tờ, cùng lề | mỗi khối một thẻ riêng bo góc có bóng, so le trái/phải 26% |
| bảng hàng | kẻ ô đầy đủ | bỏ hết kẻ, chỉ còn kẻ chân dòng nhạt |
| bộ chữ | chữ có chân | chữ không chân, dòng thoáng hơn |

Khối được xếp lại: `after` words → signatures

Ảnh: `v2_bang_thanh_the.jpg` — proof: `proof/form_timesheet_grid/v2_bang_thanh_the.jpg`

## `v3_hai_cot_so_le`

**Khối lẻ dồn trái, khối chẵn dồn phải, bảng trải hết**

Đo được: **22%** số ô chữ đã đổi chỗ so với phôi (18/82 ô ghép được, 0 ô chỉ có ở một bên; đã trừ độ dịch chung [291.8, -9.7]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| kiến trúc trang | mọi khối trải hết bề ngang | khối lẻ chiếm 2/3 bên trái, khối chẵn 2/3 bên phải, so le nhau |
| bảng hàng | thụt theo khung | trải hết bề ngang, phá thế so le |
| thứ tự khối | tiêu đề trên letterhead | letterhead lên trước, số tiền bằng chữ xuống dưới chữ ký |

Khối được xếp lại: `swap` letterhead → doctitle; `after` words → signatures

Ảnh: `v3_hai_cot_so_le.jpg` — proof: `proof/form_timesheet_grid/v3_hai_cot_so_le.jpg`

## `v4_bang_dan_dau`

**Bảng hàng lên đầu trang, khối nhận diện xuống dưới**

Đo được: **91%** số ô chữ đã đổi chỗ so với phôi (75/82 ô ghép được, 0 ô chỉ có ở một bên; đã trừ độ dịch chung [-16.2, -32.5]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| thứ tự đọc | nhận diện → bảng → tổng → ký | bảng → tổng → nhận diện → ký: đảo hẳn trục dọc của tờ giấy |
| canh khối | mọi khối cùng lề trái | từ khối thứ ba trở đi thụt vào 18%, tạo bậc thang |
| dải tiêu đề bảng | nền xám nhạt | nền đen chữ trắng |

Khối được xếp lại: `before` table → parties; `after` words → signatures; `before` footer → signatures

Ảnh: `v4_bang_dan_dau.jpg` — proof: `proof/form_timesheet_grid/v4_bang_dan_dau.jpg`

## `v5_dan_bao_chi`

**Dàn kiểu toà soạn: khối đầu tràn mép, thân thụt sâu**

Đo được: **55%** số ô chữ đã đổi chỗ so với phôi (42/79 ô ghép được, 3 ô chỉ có ở một bên; đã trừ độ dịch chung [-30.8, -0.9]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| kiến trúc trang | một lề chung cho cả tờ | hai khối đầu tràn ra mép 6mm, phần thân thụt vào 46mm |
| khung trang | khung viền bao quanh | bỏ khung, kẻ ngang dày phân vùng |
| bảng hàng | lưới ô kẻ bốn cạnh | chỉ kẻ trên và dưới, trong lòng để trống |

Khối được xếp lại: `swap` letterhead → doctitle; `before` notes → table

Ảnh: `v5_dan_bao_chi.jpg` — proof: `proof/form_timesheet_grid/v5_dan_bao_chi.jpg`

## `v6_so_cai_khong_vien`

**Kiểu sổ cái: bảng tràn mép, khối chữ thụt sâu**

Đo được: **82%** số ô chữ đã đổi chỗ so với phôi (67/82 ô ghép được, 0 ô chỉ có ở một bên; đã trừ độ dịch chung [132.9, -13.3]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| kiến trúc trang | bảng và chữ cùng một lề | bảng tràn hết bề ngang, mọi khối chữ thụt vào 34mm |
| bảng hàng | lưới ô kẻ bốn cạnh | bỏ hết viền, chỉ kẻ chân từng dòng |
| cột số | cùng bộ chữ với phần còn lại | bộ chữ máy in, chữ số đều bề ngang |

Khối được xếp lại: `before` footer → signatures

Ảnh: `v6_so_cai_khong_vien.jpg` — proof: `proof/form_timesheet_grid/v6_so_cai_khong_vien.jpg`

## `v7_bang_ron_toi`

**Băng đầu trang nền tối tràn mép, thân trang thụt vào**

Đo được: **100%** số ô chữ đã đổi chỗ so với phôi (82/82 ô ghép được, 0 ô chỉ có ở một bên; đã trừ độ dịch chung [-40.0, 16.5]).

| mặt | phôi gốc | bản dựng lại |
|---|---|---|
| đầu trang | chữ đen trên giấy trắng, cùng lề với thân | băng nền xanh đậm tràn hết bề ngang, chữ đảo màu |
| kiến trúc trang | một lề chung | băng tràn mép, thân thụt 22mm, bảng thụt 6mm — ba mức lề khác nhau |

Khối được xếp lại: `swap` letterhead → doctitle; `after` words → signatures

Ảnh: `v7_bang_ron_toi.jpg` — proof: `proof/form_timesheet_grid/v7_bang_ron_toi.jpg`
