# Browser backend, CSS decides the layout (bboxes come back from the DOM).
renderer: html

scale: 1.0
seed: 42

engine: playwright
layout: flow

font_family: "DejaVu Sans, Liberation Sans, Arial, sans-serif"
font_size: 22
title_font_size: 40
margin: 60
line_height: 1.35

# Same sheet as configs/synthdog_default.yaml.
paper:
  enabled: true
  color: [250, 249, 245]
  grain: 4.0
